def wordCounter(sen):
    # collect sentence inpul
    sen = input('Enter a sentence: ')
    # use split to convert sentence to arrray of words
    sen = sen.split()
    #length of arrary
    sen = len(sen)
    print(sen)
    return
wordCounter('')